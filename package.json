{
  "name": "react-polarisshop",
  "version": "1.0.0",
  "description": "ecommerce store with react, shopify polaris, graphql",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "json-server --watch --port 4000 db.json",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run db\" \"npm run client\""
  },
  "author": "vp",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "json-server": "^0.16.2"
  }
}
